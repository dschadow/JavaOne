CREATE TABLE employees (emp_id INTEGER, name VARCHAR(30), job_title VARCHAR(30), department VARCHAR(30), entry_date VARCHAR(10));
CREATE TABLE accounting (acc_id INTEGER, emp_id INTEGER, salary INTEGER);
